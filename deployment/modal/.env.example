MODAL_URL = "Replace with your Modal Endpoint URL"
# Best approach is to store the system prompt in a separate file and read it in your code, 
# rather than putting the entire prompt directly in the .env file. 
# This keeps the .env file cleaner and allows for easier editing of the prompt without needing to modify environment variables.
# SYSTEM_PROMPT=SYSTEM_PROMPT.txt